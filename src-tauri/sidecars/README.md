The release build places the PyInstaller `--onedir` worker here as:

`voxtral-worker-aarch64-apple-darwin/voxtral-worker`

The directory is generated by `scripts/build-voxtral-sidecar.sh` and bundled as
a Tauri resource. Model weights are never placed here or in the app bundle.
